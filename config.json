{
  "experiment_name": "test",
  "model": "dummy",
  "model_config": {
    "pretrained": true
  },
  "training": {
    "batch_size": 64,
    "max_epochs": 2,
    "num_workers": 2
  },
  "optimizer": "Adam",
  "optimizer_config": {
    "lr": 1e-4
  },
  "criterion": "MSE",
  "criterion_config": {},
  "scheduler": "ReduceLROnPlateau",
  "scheduler_config": {
    "mode": "min",
    "factor": 0.5,
    "patience": 5,
    "min_lr": 1e-7
  }
}

